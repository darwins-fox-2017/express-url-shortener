<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>
  <body>
    <h1><%= title %></h1>

    <table class="table">
      <thead>
        <tr>
          <th>No</th>
          <th>URL LINK</th>
          <th>SHORTENER LINK</th>
          <th>CLICK COUNT</th>
        </tr>
      </thead>
      <tbody>
        <% for (var i=0;i < data.length;i++) { %>
        <tr>
          <th scope="row"> <%= i + 1 %> </th>
          <!-- <td><%= data[i].id %></td> -->
          <td><a href="<%= data[i].url %>"  target="_blank"><%= data[i].url %></a></td>
          <td><a href="/url/<%=data[i].shortener_link%>" target="_blank"> <%= data[i].shortener_link %> </a></td>
          <td><%= data[i].click %></td>
        </tr>
        <% } %>
      </tbody>
    </table>

    <form action="/" method="post">
      <div class="form-group">
        <label for="url">url link:</label>
        <input name="url" type="text" class="form-control" placeholder="url link" value="<%= data.url %>">
      </div>

      <input type="submit" value="Submit" class="btn btn-success">
    </form>
  </body>
</html>
